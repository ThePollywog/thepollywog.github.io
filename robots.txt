# thepollywog.github.io/robots.txt
#
# THIS FILE GOVERNS THE WHOLE ORIGIN, not just this page. robots.txt is fetched
# from the domain root and nowhere else, so on a GitHub Pages *user* site this
# one file is also the crawl policy for /saltdog/ and /webnavfit/ — a project
# repo cannot publish its own. That is why the Sitemap line below points at a
# sitemap listing all three, and why nothing here is scoped with a path prefix.
#
# Everything is crawlable. There is no admin surface, no user content, no
# duplicate-parameter mess, and no reason to spend a Disallow.
#
# Two Sitemap lines, which is allowed and is the honest split. The root sitemap
# lists the three sites and is hand-maintained here; the second is GENERATED by
# SALTDOG's own build, which is the only thing that knows which static reference
# pages it just emitted and when their content last changed. A sitemap may only
# list URLs at or below its own path, so /saltdog/sitemap.xml is where those have
# to be declared — this file is how a crawler learns it exists.

User-agent: *
Allow: /

Sitemap: https://thepollywog.github.io/sitemap.xml
Sitemap: https://thepollywog.github.io/saltdog/sitemap.xml
